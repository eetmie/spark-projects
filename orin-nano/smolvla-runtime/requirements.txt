# SmolVLA Orin-Nano runtime — Python deps for JetPack 6 / L4T R36.4.x / Python 3.10 / aarch64.
#
# IMPORTANT: create the venv with --system-site-packages so the JetPack-provided
# `tensorrt` (10.3) and the librealsense-built `pyrealsense2` are visible:
#
#     python3 -m venv --system-site-packages .venv
#     source .venv/bin/activate
#     pip install -r requirements.txt
#
# tensorrt itself is NOT pip-installed here — it ships with JetPack. Confirm with:
#     python -c "import tensorrt; print(tensorrt.__version__)"   # -> 10.3.0

numpy<2                 # Jetson CUDA wheels expect numpy 1.x
cuda-python>=12.6,<13   # device buffers for the pure-TRT runtime; pin to the CUDA 12.x series
                        # (13.x targets CUDA 13 and breaks against this 12.6 toolkit). Import via
                        # `from cuda.bindings import runtime` (cuda.cudart is deprecated).
transformers            # tokenizer only (AutoTokenizer for the language instruction)
pillow                  # quality image resize in preprocess

# --- ort backend only (diagnostic / fallback) ----------------------------------
# onnxruntime-gpu has NO aarch64 GPU wheel on PyPI — use the Jetson AI Lab index:
#     pip install onnxruntime-gpu==1.24.0 --extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126
